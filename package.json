{
  "name": "page-apps-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "pnpm validate:data && pnpm test && pnpm build",
    "test": "vitest run test",
    "test:e2e": "playwright test",
    "validate:data": "node scripts/validate-registry.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "7.2.0"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "vitest": "4.1.0"
  }
}
